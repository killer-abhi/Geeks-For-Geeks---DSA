char firstPrecedence = getPrecedence(infix.top());
                // char secondPrecedence = getPrecedence(x);
                // if (secondPrecedence > firstPrecedence)
                // {
                //     infix.push(x);
                // }
                // else if (secondPrecedence < firstPrecedence)
                // {
                //     while (secondPrecedence < firstPrecedence)
                //     {
                //         cout << infix.top();
                //         infix.pop();
                //         secondPrecedence = getPrecedence(infix.top());
                //     }
                //     infix.push(x);
                // }
                // else
                // {
                //     cout << infix.top();
                //     infix.pop();
                //     infix.push(x);
                // }